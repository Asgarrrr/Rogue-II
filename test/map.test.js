
import assert from "assert";
import { Map } from "../src/class/-Map.js";


/* —— Test 1 ———————————————————————————————————————————————————————————————————
/ Test that the map is created with the correct dimensions and that the tiles are
/ created with the correct coordinates.
/ ————————————————————————————————————————————————————————————————————————————— */

// describe( "Map", function() {

//     describe( "#constructor", function() {

//         it( "should create a map with the correct dimensions", function() {

//             const map = new Map( );
//             map.generate( 30, 30 );

//             console.log( map )

//         });

//     });

// });


describe('Array', function () {
    describe('#indexOf()', function () {
        it('should return -1 when the value is not present', function () {
            assert.equal([1, 2, 3].indexOf(4), -1);
        });
    });
});